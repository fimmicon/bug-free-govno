node("dind") {

        stage('build') {
		sh 'touch devops'
		if (fileExists("devopsasdasd"))
			println("devops exists")
		
		if (fileExists("devops/."))
			println("devops/ exists")
		
       }

}
